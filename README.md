# 记忆代理人 - 传说武器锻造台

这是为「记忆代理人 x 落花星雨」服务器制作的专属武器定制网页。玩家可以在这里可视化设计自己的传说武器，系统会自动计算价格并生成配置代码。

## 🎮 玩家使用指南

1. **填写信息**：输入你的游戏 ID，选择想要的武器材质（如钻石剑、下界合金斧等）和自定义名称。
2. **定制属性**：
   - 在左侧选择需要的属性（攻击、防御、特殊效果等）。
   - **计费公式**：
    > 系统采用**平滑递增的凸函数**定价（无阈值、越高越贵），防止数值膨胀；部分属性支持“区间输入”，区间会按凸函数的**期望价格**计费（更科学，区间越宽自然越贵）。
     
     **最终价格计算：**
     ```
     最终价格 = ceil(max(50, ∑(单项价格 × 部位倍率)))
     ```
     *(即：总价最低 50 元起步，所有属性价格加总后向上取整)*

     **单项价格计算逻辑**：
     1. **通用（非区间）属性**：使用“进阶定价曲线”
        ```
        单项价格 = val × costFactor × (1 + (val / curveRef) ^ gamma)
        ```
        - `val`：属性数值（百分比属性同样按数值计算，例如 10% => val=10）
        - `costFactor`：属性单价系数（写在属性配置里）
        - `curveRef`：曲线参考点
        - `gamma`：曲线幂指数（目前为 1.0）
     
     2. **支持区间的属性**（目前包括：伤害 / PVE攻击 / PVP攻击 / 真实攻击 / 火焰攻击 / 雷电攻击 / 防御 / 生命恢复）
        - 输入格式示例：`1000-2000` / `1000~2000` / `1000～2000`
        - **科学定价**：把区间理解为“数值在区间内均匀波动/随机”（\(V \sim U(min, max)\)），按期望价格计费：
          ```
          单项价格 = E[ val × costFactor × (1 + (val / curveRef) ^ gamma) ] ,  val ~ Uniform(min, max)
          ```
        - *(直观理解：在凸函数下，区间越宽，不确定性越大，期望价格自然更高；无需额外手工宽度惩罚。)*
     
     **参数示例**：
     - **部位倍率**：副手 = 2.0，其他部位 = 1.0
     - **最低消费**：50 元
3. **提交订单**：
   - 满意后点击右侧的 **「复制配置代码」** 按钮。
   - 将复制的内容直接发送给服主进行实装。

---
*(开发备注：`index.html` 为混淆后的发布版本，`index.dev.html` 为未混淆的源码备份)*

## 一键生成混淆/发布版

> 目标：把 `index.dev.html` 生成新的发布文件 `index.html`（**JS 混淆 + HTML/CSS 压缩**）。

- 混淆使用 `javascript-obfuscator`，压缩使用 `html-minifier-terser`（首次运行会通过 `npx` 自动下载所需工具）

```bash
node tools/build-obfuscate.cjs
```

- 最强混淆（更慢、也更可能遇到兼容问题）：

```bash
node tools/build-obfuscate.cjs --high
```
